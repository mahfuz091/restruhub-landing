# Server-side API origin used by fetchPlans() — must be absolute.
NEXT_SERVER_API_URL=https://api.restruhub.com/api
# Legacy alias, still read as a fallback.
NEXT_PUBLIC_SERVER_API_URL=https://api.restruhub.com/api
NEXT_PUBLIC_DASHBOARD_URL=https://dashboard.restruhub.com
